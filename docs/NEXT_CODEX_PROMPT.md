# Next Codex package — schema-v3 composition core

The truthful rendered-comparison checkpoint is the prerequisite. Confirm its
branch is merged, CI is green, installed-app worksheet is sealed, and the
canonical 12-frame Natural Dissolve has fresh returned evidence or the recorded
exact blocker before beginning.

Implement the first bounded slice of `docs/SCHEMA_V3_COMPOSITION_DESIGN.md`:

1. Add schema-v3 typed graph models for a locked single clip, ordered operation
   nodes, and one locked-edit-point transition target.
2. Support enable/disable and reorder as immutable new graph versions.
3. Define deterministic same-channel conflict refusal/reduction semantics.
4. Compose one existing native node and one existing rendered node without
   adding an effect or broadening Final Cut admission.
5. Reuse `AdmittedTreatmentPreview` at graph-output identity; do not invent a
   second rendered-preview lifecycle.
6. Validate director control before preview and export.
7. Add explicit schema-v2 quarantine and a lossless one-node migration test;
   never silently reinterpret a v2 plan.
8. Record durable graph version and export history identities.

Keep imported edit manifests/FCPXML read-only. Do not add timeline mutation,
generative providers, uploads, paid calls, a fifth effect, or new codec,
duration, geometry, or Final Cut compatibility claims.

Read first: `STATUS.md`, `AGENTS.md`,
`docs/ADMITTED_PREVIEW_ARTIFACT_LIFECYCLE.md`,
`docs/SCHEMA_V3_COMPOSITION_DESIGN.md`, and
`docs/editorial-intelligence/DIRECTOR_CONTROL_CONTRACT.md`.
